query getItemsByYear($year: Int!, $type: String!, $half: Int!) {
  getItemsByHalfYear(year: $year, type: $type, half: $half) {
    title
    date
    technique
    description
    height
    width
    length
  }
}
